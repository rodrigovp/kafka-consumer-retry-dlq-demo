kafka.bootstrap.servers=localhost:9092

kafka.topic=orders
kafka.group.id=order-consumer-group

kafka.dlq.topic=orders-dlq
kafka.retry.max.attempts=3
kafka.retry.backoff.ms=1000
kafka.producer.acks=all
